<odoo>
    <record id="settlement_settlement_view_form" model="ir.ui.view">
        <field name="name">settlement_settlement_view_form</field>
        <field name="model">settlement.settlement</field>
        <field name="inherit_id" ref="cf_ded_base.settlement_settlement_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='npc_ids']" position="after">
                <field name="hex_ids" widget="many2many_tags"/>
            </xpath>
        </field>
    </record>
</odoo>
